<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/README.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/README.md" />
              <option name="originalContent" value="# order-service-iac-cicd&#10;&#10;이 저장소는 Terraform + AWS ECS + GitHub Actions를 이용한 코드 기반 인프라(CICD) 실습용 예제입니다. 주요 구성 요소:&#10;&#10;- `order-service`: Spring Boot 기반 주문/상품 서비스&#10;- `infra/terraform`: AWS와 연동되는 Terraform 구성&#10;- `.github/workflows`: CI/CD 워크플로우 (빌드/테스트, Docker 빌드/푸시, Terraform plan/apply)&#10;&#10;1) 로컬 개발&#10;- 로컬 DB(Postgres)는 `docker compose up --build`로 띄우고, `SPRING_DATASOURCE_*` 값을 맞춥니다. (이 프로젝트의 `docker-compose.yml`은 환경변수를 각 서비스의 `environment:`에 인라인으로 설정하도록 구성되어 있습니다 — 즉 `.env` 파일 없이 동작합니다.)&#10;- Gradle로 빌드 및 테스트: `order-service` 디렉터리에서 `./gradlew test` 실행&#10;- 애플리케이션 실행: `./gradlew bootRun` 또는 빌드한 JAR 실행&#10;&#10;2) GitHub Actions(요약된 변경사항)&#10;- 인증 방식: 워크플로우는 OIDC 대신 GitHub Secrets에 저장된 AWS 액세스 키(`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`)를 사용하도록 구성되어 있습니다.&#10;- Terraform Plan (`.github/workflows/terraform-plan.yml`)&#10;  - 트리거: Pull Request(또는 수동 `workflow_dispatch`)에서 실행&#10;  - 동작: `terraform plan -out=tfplan`을 실행한 뒤, 생성된 `tfplan` 파일을 아티팩트로 업로드합니다.&#10;  - 안전장치: `tfplan` 파일 존재 여부를 검증하는 스텝을 포함합니다.&#10;- Terraform Apply (`.github/workflows/terraform-apply.yml`)&#10;  - 트리거: 수동 실행(`workflow_dispatch`) — 운영 환경에 대해 수동 승인/검토 후 적용하도록 구성&#10;  - 입력: `plan_run_id`(선택) — Plan을 실행한 run id를 지정하면 해당 run의 아티팩트를 사용합니다. 비우면 최근 성공한 Plan run을 자동 검색해 사용(권장: 직접 run id를 지정하여 명확히 사용하세요)&#10;  - 동작: 지정된 Plan run에서 업로드한 `tfplan` 아티팩트를 내려받아 `terraform apply`를 수행합니다. Apply 전에 아티팩트 존재를 확인하는 스텝을 추가해 오류 원인을 명확히 합니다.&#10;- Docker 빌드/푸시 (`.github/workflows/docker-build-and-push.yml`)&#10;  - 트리거: main(또는 config에 따라) 브랜치 푸시&#10;  - 인증: AWS 액세스 키를 사용해 ECR에 로그인하고 이미지를 푸시합니다.&#10;- Build &amp; Test (`.github/workflows/build-and-test.yml`)&#10;  - PR/푸시 시 Gradle 빌드와 테스트를 수행합니다. `working-directory`가 `order-service`로 정확히 지정되어 있으므로 `./gradlew`를 올바르게 실행합니다.&#10;&#10;3) 필수 GitHub Secrets(리포지토리 → Settings → Secrets and variables → Actions)&#10;- AWS_ACCESS_KEY_ID&#10;- AWS_SECRET_ACCESS_KEY&#10;- AWS_REGION&#10;- TF_STATE_BUCKET  (Terraform 원격 상태 S3 버킷)&#10;&#10;4) 워크플로우 실행 방법(핵심)&#10;- Terraform Plan: PR을 만들거나 Actions → Terraform Plan → Run workflow로 실행&#10;  - 실행 후 Actions 페이지에서 해당 run을 열어 Artifacts에 `tfplan`이 있는지 확인하세요.&#10;- Terraform Apply(수동): Actions → Terraform Apply → Run workflow&#10;  - `plan_run_id`에 Plan 실행의 run id를 넣으면 그 run의 tfplan을 사용합니다. 비우면 최근 성공한 Plan run을 찾아 사용합니다.&#10;  - `plan_run_id` 얻는 법: Plan 실행 페이지 URL 끝의 숫자(예: .../actions/runs/12345678)&#10;- gh CLI 예시 (옵션):&#10;  - Plan list: `gh run list --workflow=&quot;Terraform Plan&quot; --limit 10`&#10;  - Apply 수동 실행: `gh workflow run terraform-apply.yml -f environment=production -f plan_run_id=&lt;RUN_ID&gt;`&#10;&#10;5) 아티팩트 'tfplan'을 다운로드할 수 없는 경우(대표 원인)&#10;- 원인 1: Plan이 `tfplan`을 생성하지 못했음 → Plan 로그의 `Terraform Plan` 스텝과 `Verify plan file exists` 스텝을 확인하세요.&#10;- 원인 2: Apply가 잘못된 run id를 참조함 → Apply 실행 시 `plan_run_id`에 명시적으로 Plan run id를 넣어 재시도하세요.&#10;- 원인 3: 포크 PR(외부 기여자)에서 실행된 Plan의 아티팩트는 base 리포지토리에서 접근이 제한될 수 있음 → maintainer가 base 리포지토리에서 Plan을 다시 실행하거나, maintainer가 수동으로 Apply하도록 정책을 정하세요.&#10;- 원인 4: 아티팩트 업로드/다운로드 권한 문제 또는 조직 정책으로 인한 접근 제한&#10;&#10;해결 팁&#10;- 항상 Plan 직후 Actions UI에서 Artifacts에 `tfplan`이 있는지 확인하세요.&#10;- 문제가 지속되면 Plan run id와 Apply 로그(특히 Verify-artifacts 스텝 출력)를 복사해 공유하면 원인 분석을 도와드리겠습니다.&#10;&#10;6) 보안 권장사항&#10;- Terraform state는 S3 + DynamoDB 잠금(선택)을 사용하세요.&#10;- Apply 권한은 GitHub Environment 보호(승인자/required reviewers)로 제한하세요.&#10;- CI용 자격증명은 최소 권한 원칙(Minimum Privilege)을 적용한 IAM 사용자/역할을 사용하세요.&#10;&#10;7) 도커 컴포즈 관련 변경&#10;- 로컬 `docker-compose.yml`에서 `.env` 대신 서비스별 `environment:`를 직접 설정해 사용하도록 변경했습니다(로컬 개발 편의). 프로덕션/CI에서는 시크릿 관리 도구를 사용하세요.&#10;&#10;8) 주의사항&#10;- 워크플로우 설정이나 Terraform 버전 등 중요한 설정을 임의로 변경하지 않았는지 확인하세요. 워크플로우 파일을 수정할 때는 사전에 검토 후 적용하시기 바랍니다.&#10;&#10;추가로 반영했으면 하는 내용이나, 워크플로우 동작 검증(Plan/Apply 실행 로그 분석)을 원하시면 Plan 실행의 run id 또는 Apply 실행 로그를 알려주세요. 제가 바로 확인해 원인 분석과 수정안을 적용해 드리겠습니다.&#10;" />
              <option name="updatedContent" value="# order-service-iac-cicd&#10;&#10;이 저장소는 Terraform + AWS ECS + GitHub Actions를 이용한 코드 기반 인프라(CICD) 실습용 예제입니다. 주요 구성 요소:&#10;&#10;- `order-service`: Spring Boot 기반 주문/상품 서비스&#10;- `infra/terraform`: AWS와 연동되는 Terraform 구성&#10;- `.github/workflows`: CI/CD 워크플로우 (빌드/테스트, Docker 빌드/푸시, Terraform plan/apply)&#10;&#10;1) 로컬 개발&#10;- 로컬 DB(Postgres)는 `docker compose up --build`로 띄우고, `SPRING_DATASOURCE_*` 값을 맞춥니다. (이 프로젝트의 `docker-compose.yml`은 환경변수를 각 서비스의 `environment:`에 인라인으로 설정하도록 구성되어 있습니다 — 즉 `.env` 파일 없이 동작합니다.)&#10;- Gradle로 빌드 및 테스트: `order-service` 디렉터리에서 `./gradlew test` 실행&#10;- 애플리케이션 실행: `./gradlew bootRun` 또는 빌드한 JAR 실행&#10;&#10;2) GitHub Actions(요약된 변경사항)&#10;- 인증 방식: 워크플로우는 OIDC 대신 GitHub Secrets에 저장된 AWS 액세스 키(`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`)를 사용하도록 구성되어 있습니다.&#10;- Terraform Plan (`.github/workflows/terraform-plan.yml`)&#10;  - 트리거: Pull Request(또는 수동 `workflow_dispatch`)에서 실행&#10;  - 동작: `terraform plan -out=tfplan`을 실행한 뒤, 생성된 `tfplan` 파일을 아티팩트로 업로드합니다.&#10;  - 안전장치: `tfplan` 파일 존재 여부를 검증하는 스텝을 포함합니다.&#10;- Terraform Apply (`.github/workflows/terraform-apply.yml`)&#10;  - 트리거: 수동 실행(`workflow_dispatch`) — 운영 환경에 대해 수동 승인/검토 후 적용하도록 구성&#10;  - 입력: `plan_run_id`(선택) — Plan을 실행한 run id를 지정하면 해당 run의 아티팩트를 사용합니다. 비우면 최근 성공한 Plan run을 자동 검색해 사용(권장: 직접 run id를 지정하여 명확히 사용하세요)&#10;  - 동작: 지정된 Plan run에서 업로드한 `tfplan` 아티팩트를 내려받아 `terraform apply`를 수행합니다. Apply 전에 아티팩트 존재를 확인하는 스텝을 추가해 오류 원인을 명확히 합니다.&#10;- Docker 빌드/푸시 (`.github/workflows/docker-build-and-push.yml`)&#10;  - 트리거: main(또는 config에 따라) 브랜치 푸시&#10;  - 인증: AWS 액세스 키를 사용해 ECR에 로그인하고 이미지를 푸시합니다.&#10;- Build &amp; Test (`.github/workflows/build-and-test.yml`)&#10;  - PR/푸시 시 Gradle 빌드와 테스트를 수행합니다. `working-directory`가 `order-service`로 정확히 지정되어 있으므로 `./gradlew`를 올바르게 실행합니다.&#10;&#10;3) 필수 GitHub Secrets(리포지토리 → Settings → Secrets and variables → Actions)&#10;- AWS_ACCESS_KEY_ID&#10;- AWS_SECRET_ACCESS_KEY&#10;- AWS_REGION&#10;- TF_STATE_BUCKET  (Terraform 원격 상태 S3 버킷)&#10;&#10;4) 워크플로우 실행 방법(핵심)&#10;- Terraform Plan: PR을 만들거나 Actions → Terraform Plan → Run workflow로 실행&#10;  - 실행 후 Actions 페이지에서 해당 run을 열어 Artifacts에 `tfplan`이 있는지 확인하세요.&#10;- Terraform Apply(수동): Actions → Terraform Apply → Run workflow&#10;  - `plan_run_id`에 Plan 실행의 run id를 넣으면 그 run의 tfplan을 사용합니다. 비우면 최근 성공한 Plan run을 찾아 사용합니다.&#10;  - `plan_run_id` 얻는 법: Plan 실행 페이지 URL 끝의 숫자(예: .../actions/runs/12345678)&#10;- gh CLI 예시 (옵션):&#10;  - Plan list: `gh run list --workflow=&quot;Terraform Plan&quot; --limit 10`&#10;  - Apply 수동 실행: `gh workflow run terraform-apply.yml -f environment=production -f plan_run_id=&lt;RUN_ID&gt;`&#10;&#10;5) 아티팩트 'tfplan'을 다운로드할 수 없는 경우(대표 원인)&#10;- 원인 1: Plan이 `tfplan`을 생성하지 못했음 → Plan 로그의 `Terraform Plan` 스텝과 `Verify plan file exists` 스텝을 확인하세요.&#10;- 원인 2: Apply가 잘못된 run id를 참조함 → Apply 실행 시 `plan_run_id`에 명시적으로 Plan run id를 넣어 재시도하세요.&#10;- 원인 3: 포크 PR(외부 기여자)에서 실행된 Plan의 아티팩트는 base 리포지토리에서 접근이 제한될 수 있음 → maintainer가 base 리포지토리에서 Plan을 다시 실행하거나, maintainer가 수동으로 Apply하도록 정책을 정하세요.&#10;- 원인 4: 아티팩트 업로드/다운로드 권한 문제 또는 조직 정책으로 인한 접근 제한&#10;&#10;해결 팁&#10;- 항상 Plan 직후 Actions UI에서 Artifacts에 `tfplan`이 있는지 확인하세요.&#10;- 문제가 지속되면 Plan run id와 Apply 로그(특히 Verify-artifacts 스텝 출력)를 복사해 공유하면 원인 분석을 도와드리겠습니다.&#10;&#10;6) 보안 권장사항&#10;- Terraform state는 S3 + DynamoDB 잠금(선택)을 사용하세요.&#10;- Apply 권한은 GitHub Environment 보호(승인자/required reviewers)로 제한하세요.&#10;- CI용 자격증명은 최소 권한 원칙(Minimum Privilege)을 적용한 IAM 사용자/역할을 사용하세요.&#10;&#10;7) 도커 컴포즈 관련 변경&#10;- 로컬 `docker-compose.yml`에서 `.env` 대신 서비스별 `environment:`를 직접 설정해 사용하도록 변경했습니다(로컬 개발 편의). 프로덕션/CI에서는 시크릿 관리 도구를 사용하세요.&#10;&#10;8) 주의사항&#10;- 워크플로우 설정이나 Terraform 버전 등 중요한 설정을 임의로 변경하지 않았는지 확인하세요. 워크플로우 파일을 수정할 때는 사전에 검토 후 적용하시기 바랍니다.&#10;&#10;추가로 반영했으면 하는 내용이나, 워크플로우 동작 검증(Plan/Apply 실행 로그 분석)을 원하시면 Plan 실행의 run id 또는 Apply 실행 로그를 알려주세요. 제가 바로 확인해 원인 분석과 수정안을 적용해 드리겠습니다." />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>